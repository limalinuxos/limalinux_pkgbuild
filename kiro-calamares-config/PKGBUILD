# Maintainer: Erik Dubois <erik.dubois@gmail.com>
pkgname=kiro-calamares-config
_pkgname=kiro-calamares-config
_destname1="/etc"
_destname2="/usr"
_destnamescript="/usr/bin"
pkgver=26.01
pkgrel=04
pkgdesc="calamares config for kiro"
arch=('any')
url="https://github.com/kirodubes/${_pkgname}"
license=('GPL3')
makedepends=('git')
depends=()
conflicts=(kiro-calamares-config-next)
provides=("${pkgname}")
options=(!strip !emptydirs)
source=(${_pkgname}::"git+$url")
sha256sums=('SKIP')
install='readme.install'
package() {
	mkdir -p "${pkgdir}${_destname1}"
	cp -r "${srcdir}/${_pkgname}/${_destname1}"* "${pkgdir}"
	mkdir -p "${pkgdir}${_destname2}"
	cp -r "${srcdir}/${_pkgname}/${_destname2}"* "${pkgdir}"
	mkdir -p "${pkgdir}${_destnamescript}"
	cp -r "${srcdir}/${_pkgname}/etc/calamares/pkgbuild/build-calamares" "${pkgdir}${_destnamescript}"
}
